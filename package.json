{
  "name": "slavescale",
  "version": "1.0.0",
  "description": "Daily devotion rating tracker",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "better-sqlite3": "^9.2.2"
  }
}
