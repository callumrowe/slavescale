networks:
  core:
    external: true

services:
  slavescale:
    container_name: slavescale
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - core
    restart: always
    expose:
      - "3000"
    volumes:
      - ./data:/app/data
